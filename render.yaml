services:
  - type: web
    name: expenso-backend
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: node dist/main.js
    envVars:
      - key: NODE_ENV
        value: production
